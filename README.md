# CTF Writeups

Personal writeup repository prepared locally.

Current contents:

- `NHNC-2026/xdd` — heap-overflow + max_input_vars CSP kill + XSS chain (497 pts, 0 solves)
- `NHNC-2026/I-Love-Proxy` — UDP route inject + cgid gate + courier lease forgery (3-layer chain)
- `OmniCTF/StayWild` — tar checkpoint-action RCE + disabled form bypass + nl wildcard filter bypass (67 pts, first blood)
- `SekaiCTF/EnD` — response desync + smuggled service worker + range oracle
- `SekaiCTF/Filtered-Reality` — deserialization + filter bypass
